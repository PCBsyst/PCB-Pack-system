import Link from "next/link";
import { Plus, Search } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { StatusBadge } from "@/components/status-badge";
import { certificationStateLabels, getCandidate, getCurrentCycle, jobs, statusLabels } from "@/data/mock-data";
export default function JobsPage(){return <AppShell title="Job 관리" description="규격별 Job과 접수부터 인증서 발행까지의 현재 단계를 조회합니다." actions={<Button className="bg-blue-800 hover:bg-blue-900"><Plus/>새 Job 등록</Button>}><section className="overflow-hidden rounded-lg border bg-white shadow-sm"><div className="flex flex-col gap-3 border-b p-4 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-2.5 h-4 w-4 text-slate-400"/><input className="h-9 w-full rounded-md border pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-blue-200" placeholder="Job No., 후보자명, 파트너사, 인증번호 검색"/></label><select className="h-9 rounded-md border bg-white px-3 text-sm"><option>전체 진행상태</option>{Object.values(statusLabels).map((v)=><option key={v}>{v}</option>)}</select><select className="h-9 rounded-md border bg-white px-3 text-sm"><option>전체 인증상태</option>{Object.values(certificationStateLabels).map((v)=><option key={v}>{v}</option>)}</select></div><div className="overflow-x-auto"><table className="w-full min-w-[1450px] text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold text-slate-500"><tr>{["Job No.","후보자","파트너사","인증규격 / 등급","진행상태","인증번호","인증발행일","만료일","인증상태","주 담당자"].map((h)=><th key={h} className="px-5 py-3">{h}</th>)}</tr></thead><tbody className="divide-y">{jobs.map((job)=>{const candidate=getCandidate(job.candidateId)!;const cycle=getCurrentCycle(job);return <tr key={job.id} className="hover:bg-blue-50/40"><td className="px-5 py-4 font-semibold text-blue-800"><Link href={`/jobs/${job.id}`}>{job.jobNo}</Link></td><td className="px-5 py-4"><Link href={`/candidates/${candidate.id}`} className="hover:text-blue-800">{candidate.name}</Link></td><td className="px-5 py-4 text-slate-600">{job.partnerCompany}</td><td className="px-5 py-4 text-slate-600">{job.standard}<br/><span className="text-xs text-slate-400">{job.currentGrade}</span></td><td className="px-5 py-4">{cycle?<StatusBadge status={cycle.status}/>:<span className="text-xs text-slate-400">진행 회차 없음</span>}</td><td className="px-5 py-4 font-medium text-slate-700">{job.certificationNo??"-"}</td><td className="px-5 py-4 text-slate-600">{job.certificationIssueDate??"-"}</td><td className="px-5 py-4 text-slate-600">{job.certificationExpiryDate??"-"}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${job.certificationState==="ACTIVE"?"bg-emerald-50 text-emerald-800":job.certificationState==="SUSPENDED"?"bg-orange-50 text-orange-800":job.certificationState==="WITHDRAWN"?"bg-rose-50 text-rose-800":"bg-slate-100 text-slate-600"}`}>{certificationStateLabels[job.certificationState]}</span></td><td className="px-5 py-4 text-slate-600">{job.primaryOwner}</td></tr>})}</tbody></table></div><div className="border-t px-5 py-3 text-xs text-slate-500">총 {jobs.length}건</div></section></AppShell>}
