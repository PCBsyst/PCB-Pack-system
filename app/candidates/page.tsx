import Link from "next/link";
import { Plus, Search } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { candidates, jobs } from "@/data/mock-data";
export default function CandidatesPage() { return <AppShell title="후보자" description="후보자 기본정보와 규격별 Job 이력을 관리합니다." actions={<Button className="bg-blue-800 hover:bg-blue-900"><Plus />후보자 등록</Button>}><section className="overflow-hidden rounded-lg border bg-white shadow-sm"><div className="border-b p-4"><label className="relative block max-w-xl"><Search className="absolute left-3 top-2.5 h-4 w-4 text-slate-400"/><input className="h-9 w-full rounded-md border pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-blue-200" placeholder="후보자명, 영문명, 연락처, 이메일 검색"/></label></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold text-slate-500"><tr>{["후보자명","영문명","연락처","이메일","국적","Job 수","최근 Job"].map((h)=><th key={h} className="px-5 py-3">{h}</th>)}</tr></thead><tbody className="divide-y">{candidates.map((candidate)=>{const owned=jobs.filter((job)=>job.candidateId===candidate.id); return <tr key={candidate.id} className="hover:bg-blue-50/40"><td className="px-5 py-4 font-semibold text-blue-800"><Link href={`/candidates/${candidate.id}`}>{candidate.name}</Link></td><td className="px-5 py-4 text-slate-600">{candidate.nameEn}</td><td className="px-5 py-4 text-slate-600">{candidate.phone}</td><td className="px-5 py-4 text-slate-600">{candidate.email}</td><td className="px-5 py-4 text-slate-600">{candidate.nationality}</td><td className="px-5 py-4 font-medium">{owned.length}건</td><td className="px-5 py-4 text-slate-600">{owned[0]?.jobNo ?? "-"}</td></tr>})}</tbody></table></div><div className="border-t px-5 py-3 text-xs text-slate-500">총 {candidates.length}명</div></section></AppShell>; }
