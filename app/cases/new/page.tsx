import Link from "next/link";
import { ArrowLeft, Save } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Field, controlClass } from "@/components/form-fields";
import { Button } from "@/components/ui/button";
export default function NewCasePage() { return <AppShell title="신규 인증건 등록" description="인증업무를 시작하기 위한 기본정보를 등록합니다."><form className="max-w-4xl rounded-lg border bg-white shadow-sm"><div className="border-b px-6 py-5"><h2 className="font-semibold">기본정보</h2><p className="mt-1 text-sm text-slate-500">개인정보가 아닌 개발용 가상 데이터만 입력해 주세요.</p></div><div className="grid gap-5 p-6 sm:grid-cols-2"><Field label="후보자명" required><input className={controlClass} placeholder="예: 홍길동" /></Field><Field label="Registration No." required><input className={controlClass} placeholder="예: REG-2026-005" /></Field><Field label="신청구분" required><select className={controlClass} defaultValue="최초">{["최초","갱신","등급변경","범위확대","기타"].map((v) => <option key={v}>{v}</option>)}</select></Field><Field label="인증규격" required><input className={controlClass} placeholder="예: ISO 9001" /></Field><Field label="신청등급" required><input className={controlClass} placeholder="예: Lead Auditor" /></Field><Field label="신청일" required><input type="date" className={controlClass} defaultValue="2026-08-30" /></Field><div className="sm:col-span-2 rounded-md border border-blue-100 bg-blue-50 p-4 text-sm text-blue-900"><p className="font-medium">등록 후 기본 상태</p><p className="mt-1">서류검토 대기 (DOCUMENT_REVIEW)</p></div></div><div className="flex justify-end gap-2 border-t bg-slate-50 px-6 py-4"><Button variant="outline" asChild><Link href="/cases"><ArrowLeft />취소</Link></Button><Button type="button" className="bg-blue-800 hover:bg-blue-900"><Save />인증건 등록</Button></div></form></AppShell>; }
